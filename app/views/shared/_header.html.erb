<%#= image_tag "logo.gif", size:"272x48", alt:"SenMall" %>

<%= if controller.kind_of?(Seller::Base)
    render "shared/seller_menubar" 
    elsif controller.kind_of?(Admin::Base)
    render "shared/admin_menubar"
    else 
    render "shared/menubar"
    end %>  